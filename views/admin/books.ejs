<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Viewport meta tag for responsiveness -->
    <title>Manage Books</title>
    <style>
        /* Common styles */
        body {
            font-family: Arial, sans-serif;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: url('https://images.pexels.com/photos/1850021/pexels-photo-1850021.jpeg') no-repeat center center fixed;
            background-size: cover;
            position: relative; /* Needed for footer positioning */
        }

        h1 {
            color: #4CAF50;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        a {
            color: #4CAF50;
            text-decoration: none;
            font-weight: bold;
            margin: 0 10px;
        }

        a:hover {
            text-decoration: underline;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            width: 90%;
            text-align: center;
            z-index: 1; /* Ensure content stays on top */
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #4CAF50;
            color: white;
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        /* Footer styles */
        footer {
            width: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 10px 0;
            position: fixed;
            bottom: 0;
            left: 0;
            z-index: 0; /* Send footer to the back */
        }

        footer p {
            margin: 0;
            font-size: 14px;
            animation: highlight 2s infinite alternate;
        }

        @keyframes highlight {
            from { color: #fff; }
            to { color: #4CAF50; }
        }

        @media (max-width: 600px) {
            table {
                width: 100%;
            }

            th, td {
                padding: 8px;
                font-size: 14px;
            }

            h1 {
                font-size: 24px;
            }

            a {
                display: block;
                margin: 5px 0;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Manage Books</h1>
        <a href="/admin/books/add">Add New Books</a>
        <a href="/admin/books/borrow">Borrowed Books</a> <!-- Link to borrow book form -->
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Author</th>
                    <th>Price</th>
                    <th>Status</th>
                    <th>Quantity</th>
                </tr>
            </thead>
            <tbody>
                <% books.forEach(book => { %>
                    <tr>
                        <td><%= book.id %></td>
                        <td><%= book.title %></td>
                        <td><%= book.author %></td>
                        <td><%= book.price %></td>
                        <td><%= book.status %></td>
                        <td><%= book.quantity %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <!-- Footer section -->
    <footer>
        <p>This website created by Aryabata Team:  [ Dhanavanthesh S, Adithya Kulkarni,  Gaurav Kumar Singh]</p>
    </footer>
</body>
</html>
