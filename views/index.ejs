<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Viewport meta tag for responsiveness -->
    <title>Library Management System</title>
    <style>
        /* Common styles */
        body {
            font-family: Arial, sans-serif;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh; /* Ensure full viewport height */
            text-align: center;
            background: url('https://images.pexels.com/photos/1907785/pexels-photo-1907785.jpeg') no-repeat center center fixed;
            background-size: cover;
            position: relative; /* Needed for footer positioning */
        }

        header {
            width: 100%;
            background-color: rgba(76, 175, 80, 0.8);
            color: white;
            padding: 20px 0;
            text-align: center;
            z-index: 1; /* Ensure header stays on top */
        }

        main {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            flex-grow: 1;
            padding: 20px;
        }

        footer {
            width: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 10px 0;
            position: absolute;
            bottom: 0;
            left: 0;
        }

        footer p {
            margin: 0;
            font-size: 14px;
            animation: highlight 2s infinite alternate;
        }

        @keyframes highlight {
            from { color: #fff; }
            to { color: #4CAF50; }
        }

        a {
            display: inline-block;
            margin: 10px 20px;
            padding: 15px 30px;
            text-decoration: none;
            color: white;
            background-color: rgba(76, 175, 80, 0.8);
            border-radius: 25px;
            transition: background-color 0.3s ease;
        }

        a:hover {
            background-color: rgba(69, 160, 73, 0.8);
        }

        a:active {
            background-color: rgba(57, 132, 60, 0.8);
        }

        @media (max-width: 600px) {
            header {
                font-size: 20px; /* Adjust header font size for small screens */
            }

            main {
                padding: 10px; /* Reduce padding for main content on small screens */
            }

            a {
                width: 80%;
                margin: 10px 0;
                padding: 15px 0;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Welcome to the Library</h1>
    </header>
    <main>
        <a href="/register">Register</a>
        <a href="/login">Member Login</a>
        <a href="/admin-login">Admin Login</a>
    </main>
    <footer>
        <p>This website created by Aryabata Team:  [ Dhanavanthesh S, Adithya Kulkarni,  Gaurav Kumar Singh]</p>
    </footer>
</body>
</html>
